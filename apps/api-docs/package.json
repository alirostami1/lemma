{
  "name": "api-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --configLoader runner",
    "check": "biome check",
    "check:write": "biome check --write",
    "dev": "vite --configLoader runner --clearScreen false",
    "format": "biome format",
    "format:write": "biome format --write",
    "lint": "biome lint",
    "lint:write": "biome lint --write",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@scalar/api-reference": "^1.55.3"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@lemma/api-contract": "workspace:*",
    "vite": "^7.0.0"
  }
}
