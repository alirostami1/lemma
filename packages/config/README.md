# @lemma/config

Shared configuration helpers for reading and validating runtime environment.

## Used By

- apps and package infrastructure adapters that need typed environment config

## Commands

```bash
pnpm --filter @lemma/config build
pnpm --filter @lemma/config check-types
pnpm --filter @lemma/config lint
```
