# @lemma/error

Shared error primitives and base classes.

## Used By

- bounded-context packages
- app error mappers

## Commands

```bash
pnpm --filter @lemma/error build
pnpm --filter @lemma/error check-types
pnpm --filter @lemma/error lint
```
