# @lemma/domain

Shared domain primitives that are intentionally generic across bounded contexts.

## Used By

- domain packages needing shared result, identifier, or validation primitives

## Commands

```bash
pnpm --filter @lemma/domain build
pnpm --filter @lemma/domain test
pnpm --filter @lemma/domain lint
```
