# @lemma/jobs

Background job dispatch and queue integration package.

## Main Surface

- job dispatcher
- PgBoss queue adapter

## Used By

- worker app
- application services that enqueue async work

## Commands

```bash
pnpm --filter @lemma/jobs build
pnpm --filter @lemma/jobs test
pnpm --filter @lemma/jobs lint
```
